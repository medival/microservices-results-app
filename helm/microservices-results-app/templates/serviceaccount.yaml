{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "microservices-results-app.serviceAccountName" . }}
  labels:
{{ include "microservices-results-app.labels" . | nindent 4 }}
{{- end -}}
