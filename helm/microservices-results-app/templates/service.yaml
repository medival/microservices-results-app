apiVersion: v1
kind: Service
metadata:
  name: {{ include "microservices-results-app.fullname" . }}
  labels: {{ include "microservices-results-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{ include "microservices-results-app.selectorLabels" . | nindent 4 }}
